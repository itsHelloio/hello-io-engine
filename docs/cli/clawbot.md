---
summary: "CLI reference for `hello-io clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `hello-io clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `hello-io clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `hello-io clawbot qr` (same behavior as [`hello-io qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `hello-io clawbot qr` -> `hello-io qr`
